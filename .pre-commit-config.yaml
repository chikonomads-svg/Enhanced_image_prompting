repos:
  - repo: local
    hooks:
      - id: check-secrets
        name: Check for leaked secrets
        entry: python scripts/check_secrets.py
        language: python
        files: ''
        pass_filenames: true
        stages: [commit]
